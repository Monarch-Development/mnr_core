local database = {}

function database.CheckBanned(identifiers)
    ---@todo Database query to check if is banned
end

function database.InsertUser(name, identifiers)
    ---@todo Database query to insert the player identifiers and name in users
end

return database